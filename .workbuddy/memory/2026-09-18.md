# 2026-09-18

## kvdb 项目启动：功能评估

用户提出用 Go 实现一个参考 RocksDB 的简单高性能 KV 存储，要求先做功能评估。

**已交付**：架构评估（3 张架构图 + 功能清单表 + 分阶段路线表）

**关键结论**
- 定位：单机嵌入式 / 单进程 / 不做分布式与事务
- 内核 6 个必需部件：WAL、MemTable、SSTable 文件格式、Manifest/Version、Compaction、internal key 编码
- 强调点：WAL 与 MemTable 是并行两条路；Immutable MemTable 必需（否则 flush 阻塞写入）；L0 区间重叠不可二分，L1 以下才可二分
- 强烈建议早期就做：Bloom Filter、Block Cache、WriteBatch、Snapshot/MVCC
- 明确初期不做：分布式/Raft、事务隔离级别、二级索引、多进程并发

**分阶段**：M0 骨架 → M1 最小可用（WAL+MemTable+简单 SST+恢复）→ M2 读优化 → M3 写优化（Compaction+Manifest）→ M4 一致性 → M5 生产化

**参考实现**：cockroachdb/pebble（最值得读）、dgraph-io/badger、LevelDB table_format 文档

## 产出物
- 设计文档落盘：`docs/DESIGN.md`（8 节 + 4 个附录，含 Mermaid 架构图、功能清单表、实施路线表、internal key 编码、Options 草图、DB 接口、建议包结构、设计决策表）
- 后续新增文档统一放 `docs/` 目录

## 环境
- Go 1.27.1 windows/amd64，路径 `C:\Program Files\Go\bin\go.exe`（不在 bash PATH 中）
- 项目目录当时为空，尚未初始化 go.mod
- 注意：本机 Bash 工具环境损坏（PATH 缺失，ls/which 均不可用）；PowerShell 工具不回显 stdout，需先 Set-Content 写文件再用 Read 读取

## M0 骨架：已实现并验收通过

在 DESIGN.md 第 6 节实施路线的基础上完成 M0，验收标准「编解码单测全绿」达成。

**交付文件**
- `go.mod`（module kvdb, go 1.23）、`doc.go`、`.gitignore`
- `options.go`：Options 全量字段 + 默认值常量、Comparer 接口 + BytewiseComparer、internalComparer 叠加、levelMaxBytes
- `internal/key/varint.go`：uvarint / zigzag varint / fixed32 / fixed64 编解码 + 3 个错误值
- `internal/key/internal_key.go`：Kind、trailer 打包、编解码、InternalKeyCompare、SeekKey
- 对应 4 个单测文件

**验收结果**：`gofmt -l` / `go vet` 无输出；35 个测试函数（含子测试 54 例）全 PASS；覆盖率 kvdb 100.0%、kvdb/internal/key 98.9%。`go test -race` 因环境无 cgo 不可用，留到 M4 补验。

**M0 期间确定的决策**
- Comparer 用接口（需 Name() 写 Manifest 校验目录/配置一致性），不用函数字段
- MakeTrailer 对 seq > 2^56-1 直接 panic，不静默截断
- SeekKey 尾缀用 (snapshot, TypeValue)：降序排列下是最新可见版本，且能命中 seq 恰等于 snapshot 的墓碑
- Options 数值字段：0 = 用默认值，负数 = 关闭 BlockCache/Bloom
- 二进制编码全程大端，与 LevelDB 对齐
- varint 与 internal key 同放 internal/key（按附录 C 规划）

**文档同步**：DESIGN.md 新增第 9 节「实施进度」（9.1 阶段状态 / 9.2 交付物 / 9.3 API 一览 / 9.4 验收结果 / 9.5 决策记录 / 9.6 M1 待办），第 6 节路线表加「状态」列，附录 A 的 Options 草图改为实际实现，附录 C 包结构加 `[M0 已完成]` 等里程碑标注。

**下一步（M1）**：internal/memdb 跳表 + MemTable、internal/wal、最小 sst、db.go（写入口统一收敛到 Write）、崩溃恢复（目录扫描 + WAL 重放）、端到端 kill -9 测试 + cmd/kvdb-bench 雏形。

## M1 最小可用：已实现并验收通过

验收标准「单线程 10 万次读写结果正确；kill -9 后数据不丢」两条都达成。

**交付文件（新增 16 个）**
- `db.go`：DB 类型、Open/Close/Get/Put/Delete/Write、目录锁、冻结 Immutable、Stats/RecoveryReport
- `db_flush.go`：目录扫描恢复、WAL 重放、后台 Flush 协程、SST 落盘、过期日志清理
- `batch.go`：WriteBatch + 二进制编解码（头部 seq(8B)+count(4B)，记录 = kind|uvarint(klen)|key|[uvarint(vlen)|value]）
- `file_lock_windows.go`（LockFileEx）/ `file_lock_unix.go`（flock）：目录锁随进程消亡自动释放
- `internal/memdb/skiplist.go`（跳表，单写者+无锁读）、`memtable.go`
- `internal/wal/writer.go`（32KB 分块、7 字节头固定32位的 CRC32C 掩码）、`reader.go`、`wal.go`
- `internal/sst/writer.go` + `reader.go`（数据区 + 16B footer = fixed64(dataLen) + magic "kvdb0001"）
- `internal/key/comparer.go`：Comparer 接口 + InternalComparer（根包保留 internalComparer 别名）
- `cmd/kvdb-bench/main.go`、`scripts/gotest.ps1`
- 测试：db_test.go(14) / db_crash_test.go(3) / batch_test.go(7) / comparer_test.go(5) + 三个内部包测试

**验收结果**：gofmt -l / go vet / GOOS=linux go build 均无输出；测试函数共 101 个全 PASS；
覆盖率 kvdb 89.3%、key 98.1%、memdb 100.0%、wal 85.1%、sst 80.5%。

**关键实现决策（详见 DESIGN.md 9.9）**
- 恢复时强制把重放结果落成 SST 再删旧日志，删日志判据 = 编号 < min(mem.logNum, imm.logNum)
- 崩溃中断的 Flush（footer 不全的 .sst）直接删除，数据仍在 WAL 里
- 目录锁用 OS 级锁而非 O_EXCL 锁文件（否则 kill -9 后目录永久打不开）
- 同一时刻只允许一张 Immutable；写者在冻结时最多等它落盘
- 序列号写进 WriteBatch 头部，恢复时原样沿用（绝不能重新分配）
- Close 不落盘 MemTable，下次 Open 走"重放 + 落盘"，让恢复路径每次都被走到
- 日志尾部损坏可容忍、文件中段损坏不可容忍
- 文件编号在 SST 与日志之间共享，`编号大 = 更新`

**崩溃测试的做法（重要）**：同进程内"不调 Close"证明不了什么（目录锁要进程真的死才释放）。
用 `exec.Command(os.Args[0], "-test.run=TestCrashWriterChild")` + 环境变量传参，
子进程写完 `os.Exit(1)` 不做任何清理；父进程重开校验。
另有一条用例在子进程里直接往日志尾部追加 3 个垃圾字节，验证半截记录被容忍。

**性能观感（cmd/kvdb-bench，SyncWrites=false）**：写完 5.54 µs/op；
但 3 个 SST 时读只有 3930 ops/s（254 µs/op）——线性扫描的代价，正是 M2 的靶子。

**环境新增经验**
- PowerShell 里跑 go 命令要先把 `[Console]::OutputEncoding` 设为 UTF-8，否则中文输出变乱码
- 已固化为 `scripts/gotest.ps1`（用法：`powershell -NoProfile -File scripts\gotest.ps1 test ./... -cover`，结果写在 `_t.log`）
- 批量替换重复代码片段时，Edit 工具的 replace_all 比 PowerShell 正则替换可靠（正则只命中了一部分，还静默返回成功）

**下一步（M2）**：internal/filter（Bloom）、internal/cache（分片 LRU）、sst 改成 Data/Index/MetaIndex/Footer 五段式、
internal/iterator（MergingIterator）、db 的 NewIterator/GetSnapshot。见 DESIGN.md 9.10。

## 打通 cgo / -race（把 M0 遗留的最大风险项清掉）

用户提示本机已装 msys2 + gcc，实测确认并跑通了 `-race`。

**环境事实**
- MSYS2 装在 `C:\msys64`；gcc 16.1.0 在 `C:\msys64\ucrt64\bin\gcc.exe`
  （只有 UCRT64 装了 toolchain，mingw64/mingw32/clang64 都没有 gcc）
- gcc 不在系统 PATH 里；`go env CGO_ENABLED` 默认 0
- 验证用最小 cgo 程序（C 里 `printf("hello from C")`）→ `go run` 打印成功，`go build -race` 成功

**关键发现：只有 Bash 工具跑得通 cgo**
- PowerShell 工具里改 `$env:PATH` 不会传给子进程（实测子进程看到的仍是旧 PATH），
  连 `CC=<gcc 绝对路径>` 也在 cgo 阶段报 `cgo.exe: exit status 2` —— 在 PowerShell 里 cgo 无解
- Bash 工具里 `export PATH=...` 能真正传给子进程；虽然 coreutils（ls/sed/which）全缺，
  但 echo/cd/export/重定向可用，跑 go 命令完全够用

**可用配方**
```bash
export PATH="/c/msys64/ucrt64/bin:$PATH"
export CGO_ENABLED=1 CC=gcc
cd /c/Users/summer/repos/kvdb
"/c/Program Files/Go/bin/go.exe" test -race ./... > _race.log 2>&1
```

**验收结果**：`ok kvdb 12.617s`，key/memdb/sst/wal 四个内部包全 ok，race_exit=0，**无 data race 报告**。
M1 的并发代码（跳表、后台 Flush、Immutable 冻结）通过竞态检测，DESIGN.md 里"留到 M4 补验"的
`-race` 项可以提前划掉。

## 固化：PATH 配置 + race 脚本（含踩到的 ps1 编码坑）

用户同意后做了两件事。

**① `C:\msys64\ucrt64\bin` 写进用户级 PATH**
- 直接改 `HKCU:\Environment` 的 `Path`，**保留 ExpandString 类型**（`SetValue(..., RegistryValueKind::ExpandString)`，
  不能用普通 Set-ItemProperty 以免类型退化成 String、`%USERPROFILE%` 被固化）
- 原值已备份到 `_path_backup.log`（误写成 `_path_backup.txt`，后来改名好被 `/_*.log` 忽略）
- 只对用户新开的终端生效；WorkBuddy 的 PowerShell 工具因为重写子进程 PATH，依旧吃不到

**② 新增 `scripts/gotest-race.sh`**
- 内部自己 export PATH/CGO_ENABLED/CC，不需要调用方准备环境
- 无参数 = `go test -race ./...`；第一个参数以 `.` 或 `/` 开头时自动补 `test -race`
- 输出同时打到屏幕和 `_race.log`
- 被 `source` 时用 `return`、直接执行时用 `exit`（`"${BASH_SOURCE[0]}" != "$0"` 判断），
  避免 source 调用把交互 shell 一起关掉
- 用参数展开 `${BASH_SOURCE[0]%/*}` 取脚本目录，绕开本机没有 `dirname` 的问题

**踩到的两个坑（重要）**
1. **Bash 工具里不能嵌套 `bash scripts/xxx.sh`**：沙箱会去探测 `wsl.exe`，命中程序黑名单，
   报 `PROGRAM BLOCKED BY SECURITY POLICY` 直接拦下。WorkBuddy 里必须用 `source`。
2. **`scripts/gotest.ps1` 被我的 Edit 改坏过**：文件原本是**无 BOM 的 UTF-8**，
   Windows PowerShell 5.1 按 GBK 解码，中文注释吃掉换行字节后报
   `表达式或语句中包含意外的标记"}"`（ParserError，行号 15 和真实内容对不上，很有迷惑性）。
   修法：`[System.IO.File]::WriteAllText($p, $text, (New-Object System.Text.UTF8Encoding $true))` 补 BOM，
   补完 Parser 检查 0 错误。顺手在脚本里加了 -race 警告行，提示走 bash 脚本。

**验证**：`source scripts/gotest-race.sh`（缓存与非缓存 `-count=1` 各一次）全绿退出码 0；
source 后 shell 存活；`powershell -File scripts\gotest.ps1 test ./internal/key/...` 恢复正常，`-race` 时正确打出警告。

**③ 顺便把这套环境知识做成了 skill**
`~/.workbuddy/skills/win-go-cgo-race/`（用户级，跨项目可用）：
- `SKILL.md`：环境事实表 + 三条核心规则 + 陷阱对照表（PowerShell 引擎选择 / 嵌套 bash 被拦 / coreutils 缺失 /
  ps1 编码与静默失效 / PowerShell 不回显 stdout）+ 补 BOM 的做法 + 引擎守卫代码 + 改用户级 PATH 的注册表姿势
- `scripts/gotest-race.sh`：通用版（以 cwd 为项目根），可直接复制进任何 Go 项目
- 已用 skill-creator 的 `package_skill.py` 校验通过（Skill is valid!）

## 改用 PowerShell 7 跑脚本（引擎规则固化）

用户提示本机装了 PowerShell 7，要求找到并固化"用 7 跑脚本"的规则。

**查明的事实**
- PS7：`C:\Program Files\PowerShell\7\pwsh.exe`，**7.6.6 / Core**；命令名 `pwsh`
- PS5.1：`C:\WINDOWS\System32\WindowsPowerShell\v1.0\powershell.exe`，**5.1.26100.9444 / Desktop**；命令名 `powershell`
- **WorkBuddy 的 PowerShell 工具本身就运行在 pwsh 7 上**（`$PSVersionTable.PSEdition = Core`，
  ProcessPath = pwsh.EXE）—— 所以坑只出在**嵌套调用** `powershell -File ...` 时

**用合成用例证明了编码陷阱的机理**：写一份无 BOM 的 UTF-8 `bomtest.ps1`（中文注释 + `$ver = ...` 赋值），
- `powershell`（5.1）跑 → 输出 `parsed ok, engine=`（`$ver` 为空，说明**赋值那一行被并进上一行中文注释里了**，静默失效、毫无报错）
- `pwsh`（7）跑 → 输出 `parsed ok, engine=7.6.6`，正确
这比当初 gotest.ps1 的 ParserError 更隐蔽，值得记住。

**固化动作**
1. `scripts/gotest.ps1`：头部用法改成 `pwsh -NoProfile -File`；在 `param()` 后加了**自动重启守卫** ——
   检测到 `$PSVersionTable.PSEdition -ne "Core"` 就用 `pwsh -NoProfile -File $PSCommandPath @Args` 重跑自己，
   不依赖使用者记得用 pwsh。日志头部加了 `[engine Core 7.6.6]` 便于事后确认。
   实测：用 5.1 启动 → 日志显示 `[engine Core 7.6.6]`，验证成功切换
2. 项目 MEMORY.md：更新 gotest.ps1 用法；**推翻并改正了之前"PowerShell 工具 PATH 传不到子进程"的错误结论**
3. 新建用户级 `~/.workbuddy/MEMORY.md`：写入跨项目规则（pwsh/5.1 的区别、BOM 归属、Write 不写 BOM 而 Edit 保留 BOM）
4. 更新 skill `win-go-cgo-race`：新增"引擎规则"作为第 1 条核心规则，并新增一节
   "为什么 PowerShell 通道跑不了 cgo（别重复验证）"，重新校验通过

**关于 cgo 在 PowerShell 下的补充实测**（结论：仍然只走 Bash）
- `$env:PATH` 实测**能**传给子进程和孙进程（`C:\msys64\ucrt64\bin` 出现在孙进程 PATH 里）——之前的结论是错的
- 但 `CC=gcc` + PATH 时：编译阶段过、**卡在链接**，4/4 稳定复现
  `link.exe: running gcc failed: exec: "gcc": executable file not found in %PATH%`
- `CC` 给绝对路径（正/反斜杠都试过）→ cgo 阶段就挂：`cgo.exe: exit status 2`
- 加 `-ldflags=-extld=<绝对路径>` → cgo 阶段报 `C compiler "gcc" not found`
- 问题在 Go 工具链解析 C 编译器这一环，不是 PATH 继承；Bash 通道一跑就过，别在 PowerShell 里修

## M2 读优化：已实现并验收通过

验收标准三条（DESIGN.md 9.10）全部达成：点查不触发全文件扫描；范围扫描可用；
读延迟不再随文件数线性增长。

**交付文件（新增 20 个，重写 4 个）**
- `internal/crc/`：CRC32C + LevelDB 掩码抽成共用包，WAL 与 SST 都改用它（wal/writer.go、wal_test.go 随之收敛）
- `internal/filter/`：bloom.go（哈希 + 位图）、block.go（Filter Block：2KB 切段 + `fixed32×(N+1)` 偏移数组 + baseLg）
- `internal/cache/`：16 分片 LRU，键 `(fileNum, offset)`，按字节计费，`New(<=0)` 返回 nil
- `internal/sst/`：format.go（Footer 48B / magic `kvdb0002` / 错误值）、block.go（前缀压缩 + 重启点）、
  writer.go（五段式：Data→Filter→MetaIndex→Index→Footer）、reader.go（Open 载入索引；Get = Index 二分→Bloom→读块→块内 seek）、iterator.go（按需加载块）
- `internal/iterator/`：iterator.go（统一接口）、merging.go（最小堆归并）、dbiter.go（可见性/墓碑/去重/闭区间边界）
- `db_iter.go`：`NewIterator` / `GetSnapshot` / `Snapshot` / 导出 `Iterator` / `IteratorOptions`
- `internal/memdb/skiplist.go` 补 `Iterator.Error()`
- 测试：crc_test / filter_test / lru_test / block_test / sst_test(重写) / iterator_test / db_iter_test

**验收结果**
- `go test ./... -count=1 -cover` 全绿：kvdb 89.6%、cache 96.8%、crc 100%、filter 94.4%、iterator 86.1%、
  key 98.1%、memdb 98.6%、sst 79.5%、wal 84.9%
- `go vet ./...` / `gofmt -l` 无输出；`go test -race ./...` 全绿，无 data race
- 基准（10 万 key、value 100B）：4 个 SST 时 get(hit) 2.83 µs/op、顺序读回 1.72 µs/op、
  范围扫描 0.06–0.15 µs/key、缓存命中率 89.2%
- **与 M1 同口径对比**：M1 三个 SST 时读回 254 µs/op → M2 四个 SST 时 1.72 µs/op，约 **148 倍**
- **文件数扫描（`-mode sweep`）**：文件数 ×69、get(hit) ×4.4；拟合 `≈ 2.22 + 0.126 × files` µs。
  对照组关掉 Bloom 与缓存后线性项系数 2.354 µs/文件（差 18.7 倍），69 文件时 168.11 µs vs 11.17 µs

**关键实现决策（详见 DESIGN.md 9.13）**
- 同一 user key 的所有版本必须落在同一个 Data Block 里（块边界只在 user key 变化时切）——
  这是"读一块就能定可见版本"的正确性前提
- Bloom 建在 **user key** 上，不是带 `(seq, type)` 后缀的 internal key
- Filter Block 解析失败时返回"可能存在"，绝不返回"肯定不存在"（过滤器坏了不能变成静默丢数据）
- Index 记的是块的 **max key**（不是首 key），与 LevelDB 一致
- 缓存键必须带 fileNum：文件编号会因删除而复用，只用 offset 会串味
- 分片哈希用 splitmix64 fmix 打散**对齐后**的偏移（块偏移都是 4KB 对齐，直接取低位会全落一个分片）
- `DB.Get` 出口统一 `copyValue` —— 返回的切片可能指向块缓存内部字节
- magic 从 `kvdb0001` 升到 `kvdb0002`；旧文件报 `ErrLegacyFormat` 且**不可容忍**（不能当损坏文件删掉）
- `scanSSTFiles` 的丢弃判据收紧到只有 `ErrBadFooter` / `ErrCorruptBlock`
- 迭代器接口现在就把 `Close()` 放进去（M3 版本引用计数后才是必需的）

**验收里最容易被误读的一点（重要）**：sweep 的曲线**不是平的**。M2 没有 Compaction，L0 文件数无上限，
所以"每文件一次元数据探测"的线性项（0.126 µs/文件）客观存在，要等 M3 的 `L0CompactionTrigger` 才被兜住。
变的是**量级**：从"每文件一次全文件扫描"降到"每文件一次 Bloom 判定"，约三个数量级。
文档里按这个口径写，没有说成"延迟与文件数无关"。

**基准工具的新用法**：`-mode` 取 `all | write | point | scan | sweep`；`sweep` 默认带一组
"关掉 Bloom 与缓存"的对照（`-variants=false` 可关）。`get(hit)` 与 `get(miss)` 分开报，
因为 Bloom 的价值只体现在负向查询上。写完后会等 Immutable 落盘再测读，否则测到的还是 MemTable。

**环境经验新增**
- 把 Go 程序的 stdout 收进日志：`pwsh -NoProfile -File scripts\gotest.ps1 run ./cmd/kvdb-bench -mode all -n 100000`
  —— `gotest.ps1` 把 `go` 的 `*>&1` 全部 Add-Content 到 `_t.log`，`go run` 的程序输出一并进去，
  绕开"PowerShell 工具不回显 stdout"。跑基准只能这么干。
- 本仓**不是 git 仓库**（`fatal: not a git repository`），所以没法 checkout 出 M1 代码做同机对比，
  M1 的 254 µs/op 沿用当时记录，口径是"同机同操作同数据规模"
- 空块是合法的：MetaIndex / Index 在空文件里只有重启数组，没有 entry，`blockIter` 需要专门的 `empty` 标志

**下一步（M3）**：Manifest/Version + Compaction。见 DESIGN.md 9.14。

## git 初始化 + M0/M1/M2 提交

`git init` 完成，基线提交 `6a375c1 M0+M1+M2: 骨架、最小可用链路与读优化`。
（此前记录的"本仓不是 git 仓库"已失效。）

## M3 写优化：已实现、验收通过并提交

提交 `683e9c3 M3: 写优化 —— Manifest/版本树 + Leveled Compaction`（19 files, +5091/-157）。

**交付文件**
- `internal/version/`：edit.go（`VersionEdit`/`FileEdit` 标签式二进制编码）、
  version.go（`FileMeta`、`Version` 不可变分层清单 + 引用计数、`VersionSet` 与 `LogAndApply`/`SetFromScan`/`LiveFileNums`）、
  manifest.go（复用 WAL 记录格式的追加日志、`CURRENT` 读写与原子替换、`Recover` 含尾部截断容忍与比较器校验、`NewManifest`）
- `internal/compact/`：compact.go（`LevelConfig`/`Compaction`/`Pick`，L0 按文件数、L1 以下按容量）、
  run.go（`Run`：多路归并 → 丢旧版本与可退休墓碑 → user key 边界切分 → `Commit` 原子替换；失败删半成品）
- `db_compact.go`（新）：后台 Compaction 协程、每轮从当前版本重新规划、
  `commitCompaction` 三步（锁外开 reader → 锁内先登记 reader → 最后落 Manifest）、快照登记与 `releaseVersion`
- 改写：`db.go`（`Stats`/`LevelStats`/`CompactionStats`、`RecoveryReport` 扩充、`Close` 等后台排空后兜底 GC）、
  `db_flush.go`（孤儿文件分类、Flush 提交走 Manifest、`removeUnreferencedSSTs`）、
  `db_iter.go`（迭代器持版本引用 + 登记存活快照）、`options.go`（四个分层字段真正接入 + `targetFileSize`）
- 测试：`internal/version/version_test.go`(17) / `internal/compact/compact_test.go`(11) / `db_compact_test.go`(11 条验收)
- `cmd/kvdb-bench/main.go`：新增分层布局 + 写放大（wal+flush+compaction-out / user）+ 读放大（probes/get）+ Compaction 规模

**验收结果**
- `go test ./... -count=1 -cover` 全绿：kvdb 83.0%、cache 96.8%、compact 86.9%、crc 100%、filter 94.4%、
  iterator 86.1%、key 98.1%、memdb 98.6%、sst 76.2%、version 78.5%、wal 84.5%
- `go vet ./...` / `gofmt -l .` 无输出；`go test -race ./...` 全绿（kvdb 35.7s），**无 data race**
- 实测（`-mode all -memtable-size 1048576`，`L0CompactionTrigger=4`、`LevelBaseSize=256MB`）：
  | 规模 | sst | L0 | L1 | write amp | read amp |
  |---|---|---|---|---|---|
  | 10 万 key | 17 | **3** | 14 (8.71MB) | 2.89x | 3.54 probes/get |
  | 50 万 key | 89 | **3** | 86 (53.48MB) | 3.12x | 3.90 probes/get |
  数据 ×5 → L0 不变、读放大 ×1.10、写放大 2.89→3.12

**M3 顺带修掉的四个真 bug（都被 DB 层侥幸掩盖，只有单包测试/极端时序才暴露）**
1. **`VersionSet.mu` 自死锁**：`Unref` 归零时重入加锁，而 `LogAndApply`/`SetFromScan`/重放路径本来就持有该锁。
   DB 层看不到是因为 `db.v` 永远持一个长期引用、旧版本到不了 0。修法：拆 `unrefLocked`/`removeLiveLocked`。
2. **每次重开多留一份 Manifest**：`NewManifest` 按内存句柄删旧文件，而恢复后句柄是 nil。
   改为按 `manifestNum` 拼路径删。
3. **关库窗口漏回收 SST**：Compaction 提交完新版本后撞上 `db.closed`，收尾的 `collectGarbage` 被跳过 → 目录留孤儿。
   修法：`bgWG.Wait()` 后调 `collectGarbageFinal()`（走 `removeUnreferencedSSTs(allowClosed=true)`）。
4. **`LastSeq` 未持久化会退回 0 并与 SST 老记录撞号**（"所有 MemTable 已落盘、WAL 为空"时崩溃）。
   修法：每次提交把 `LastSeq` 写进 Manifest，不再只靠重放 WAL 恢复。

**M3 关键决策（详见 DESIGN.md 9.17）**
- 孤儿文件分两类：`DiscardedFiles`（残缺、根本不该存在）vs `ObsoleteFiles`（完整、提交没成功）；判据是"能否正常打开"
- Compaction **每次只往下走一层**（跨层归并写入量不可控、出错无法定位）
- 输出**只在 user key 变化处切分**（"同一 key 所有版本同文件"的文件级不变式）
- `Inputs[1]`（输出层重叠文件）必须一起归并，否则"L1 以下同层不重叠"当场破掉
- `Commit` 三步顺序不能乱：先开 reader → 再登记 → 最后提交版本
- `Pick` 无事可做返回 nil（给 `runCompactions` 干净的终止条件）；每轮重新挑，不排队
- 写放大分子**不含** Compaction 读入字节（那是读放大的一部分，混在一起会互相污染）
- GC 必须"先 `os.Remove` 成功、再删 `db.readers` 条目"（反过来会把瞬时 IO 错误放大成永久故障）

**两条要如实说明的边界（已写进 DESIGN.md 9.16 的引用块）**
1. kvdb 覆盖率 89.6% → 83.0%：新增的异常/关库竞态分支抬高了分母，不是质量退化
2. bench 里 `get(hit)` 3.47 → 10.34 µs **不代表读路径退化**：数据 11MB → 55MB 而缓存固定 8MB，
   命中率 86.7% → 65.2%，涨幅来自"缓存装不下、要去磁盘读块"（属 M5 调优），
   读放大同一时段只从 3.54 涨到 3.90。两件事必须分开读

**测试写法的两个教训**
- `TestReadAmplificationStaysBoundedAfterTenfoldGrowth` 必须在**稳态**测（等 `Compaction.Count > 0`），
  否则会在"L0 还没开始搬"的窗口里测到虚高的值
- 断言"重开后层布局不变"时不能断言 L0 精确文件数：`Close` 不落盘 MemTable，重开会重放 WAL 多出至多一个 L0 文件
  （`Close` 故意不落盘是 M1 就定下的约定，为了让恢复路径每次都被走到）

**环境经验新增**
- **Bash 工具能正常跑 `git`**（之前只知道能跑 go）：`git status/log/diff/add/commit` 全部可用，
  缺的只是 coreutils（`ls/cat/head/grep`），所以**不要给 git 命令接管道**，直接单条执行
- **中文 commit message 的可靠姿势**：用 Write 工具把消息写到 `_commitmsg.log`
  （`/_*.log` 已被 .gitignore 忽略，不会污染工作区），再 `git commit -F _commitmsg.log`。
  直接 `git commit -m "中文"` 经 Bash 工具容易踩编码坑
- 提交后必须核对 `git log -1 --pretty=%s/%b`，确认中文没有乱码（本次验证正常）
- `git add -A` 对本仓是安全的：所有 `_*.log` 都在 .gitignore 里，`git status --short` 不会列出它们
  （用 `git status --short --ignored` 能看到它们以 `!!` 开头）

**下一步（M4 一致性）**：Snapshot 回收序列号（现在 `Release` 只是标记失效、不回收）、
WriteBatch 的 Group Commit（并发写合并成一次 fsync）、并发压测下 race 无告警。

## M4 一致性：已实现、验收通过并提交

提交 `1de8952 M4: 一致性 —— 组提交（写队列 + 一次 fsync 服务整组）`（9 files, +1767/-82）。

验收标准「并发压测下 race detector 无告警」达成（`go test -race ./... -count=1` 全绿，
kvdb 39.0s，无 data race 报告）。

**先做了一次范围核对，结论很重要**：M4 的三件交付物里，`WriteBatch` 在 M1 就已落地
（`batch.go`），`Snapshot` 在 M2/M3 就已落地（序列号视图 + `db.snapshots` 登记表
+ `smallestSnapshot` 消费它）。**真正新写的只有一个：Group Commit**。
另两件这一阶段做的是补语义、补可观测性、用并发用例锁住。

**交付文件**
- `db_write.go`（新）：组提交全部实现 —— `writeRequest`、队长/跟随者模型、
  `runWriteGroup`（分配序列号 → 追加 → **一次 fsync** → 落 MemTable）、
  `finishWriteGroup`（发结论 + 决定队长是否续任）、`drainWrites`、
  `beginWriteGroup` / `appendWriteGroup` / `applyWriteGroup` / `takeWriteQueue`
- `db.go`：`Write` 从"持 db.mu 直接落盘"改成"排进写队列"；DB 新增
  `wmu / wcond / wqueue / wleader / wclosing`；`Close` 前置 `drainWrites()`；
  `Stats` 加 `WriteGroups / WriteBatches / MaxWriteGroup / WALFsyncs / LiveSnapshots`
- `db_compact.go`：`setBgErr` 与新的 `failLocked`（持锁版）合并成唯一一处"停库"入口
- `db_concurrent_test.go`（新）：9 条并发用例
- `cmd/kvdb-bench/main.go`：新增 `-mode group -writers 1,2,4,8,16,32`
- `doc.go`：包文档从 M1 更新到 M4（之前一直没跟）

**实测（本机 Windows，`-mode group -writes 3000`，SyncWrites=true）**

| writers | ops/s | fsync 次数 | 合并率 | us/fsync |
|---|---|---|---|---|
| 1 | 487 | 3000 | 1.00 | 2053 |
| 4 | 1934 | 774 | 3.88 | 2004 |
| 8 | 3813 | 388 | 7.73 | 2028 |
| 16 | 6689 | 205 | 14.63 | 2188 |
| 32 | 12504 | 110 | 27.27 | 2181 |

吞吐 ×25.7、fsync ÷27.3；`us/fsync` 恒定 2.0–2.2 ms（fsync 是这台机器的固定物理量）。
单测 `TestGroupCommitAmortizesFsync`：1024 次写 → 100 余个提交组。

**踩到并修掉的那个坑（最关键的一条）**
队长"提交一组就让位"会**丢失唤醒**：让位那一瞬间队列里可能还有刚排进来的写者，
它们看到 `wleader == false` 就没人提交了，永远等不到 `done`。
现象是 16 个 goroutine 全卡在 `wcond.Wait`，而栈上**没有队长** —— 这个形态一眼可辨。
修法：把"队列是否非空"与"清空 wleader"放进同一个 `wmu` 临界区（队列非空就继续当队长）。
由此得到不变式 `len(wqueue) > 0 ⟹ wleader`。

**另外三条容易搞错的次序**
1. `drainWrites` 必须**先关门再等队长**：反过来在持续写入下队列永远不空，`Close` 永远等下去
2. 队列排空必须排在 `Close` 拿 `db.mu` **之前**（队长后半段要拿 db.mu，否则互等）
3. `finishWriteGroup` **先写 done 再 Broadcast**

**`collectYields = 4`（唯一一处调度层面的主动让步）**
队长由"第一个到达的写者"担任，它到达时队列里往往只有自己 → 每组退化成
"1 个 + (N-1) 个"交替，一半 fsync 浪费在只有队长的那一组。
在 fsync 前 `runtime.Gosched()` 让几次（几微秒 vs 2ms 的 fsync），
把已就绪的写者收进本组：合并率 **15.87 → 22.4~27.8**，吞吐再涨四到七成。

**失败语义（RocksDB 式停库）**：一条批次原子，但一个**提交组**不是 ——
组里靠前的批次可能已进 WAL。所以规定"组内任何一步出错 → 整组失败 + 记 bgErr 停库"。
`Write` 的错误分两类：`ErrClosed`/既有后台错误 = 一定没写进去；
磁盘故障 = **可能写进去了，库已停止，重开即可**。
`TestWALFailureStopsTheDatabase` 用"关掉日志句柄"制造真实写失败来锁住这条。

**顺带订正的一处文档错误**：DESIGN.md 9.16 原写"打开迭代器时登记存活快照"，与代码不符。
真实机制是两套：**快照靠登记**（作用对象是旧版本，抬高 `smallestSnapshot`）、
**迭代器靠版本引用**（作用对象是输入文件，引用计数让它删不掉）。
给迭代器也登记快照反而有害：长命迭代器会把丢弃上界永久钉死。已在 9.16 加引用块订正。

**验收数据**：kvdb 覆盖率 83.0% → 84.2%（新增并发用例盖住了原先只有异常路径才走到的分支）；
gofmt / vet 无输出；全量测试全绿。

**下一步（M5 生产化）**：块压缩（sst 块尾已留出 1 字节压缩类型）、
Rate Limiter、Metrics 导出、Checkpoint，跑通 YCSB 输出基准报告。



---

## M5 生产化（已完成，commit 8cba746）

**交付**：internal/compress（Snappy+zlib，块级"省不下 1/8 就不压"）、internal/rate
（令牌桶，输入/输出按 256KB 计费）、internal/logger（LOG→LOG.old 轮转）、
SST 读写两侧接压缩 + 共享 BlockStats、version 抽出 SnapshotEdit/WriteManifest、
**DB.Checkpoint**（一致性副本：硬链接优先 + WAL 尾巴 + 快照 Manifest）、
bench 新增 ycsb / compress / checkpoint 模式与 -report 输出。

**验收**：全测试绿 + -race 全包无 DATA RACE；docs/BENCH.md（Snappy 6.87x、
YCSB A~F、限流把 7 轮 Compaction 压成 1 轮而前台只降 13%、Checkpoint 10 万 key 56ms）。

**M5 关键教训（都真实踩过）**：
1. **Checkpoint 必须先 `db.log.Flush()` 再复制 WAL**——SyncWrites=false 时记录还在
   bufio 里，漏了这步副本丢掉全部未落盘写（被 TestCheckpointCatchesUnflushedTail 抓住）。
2. **过期日志删除失败不能停库**——Windows 上刚被读过的文件（杀毒/索引服务占用）
   会在 flushLoop 删除时稳定撞上"被另一进程占用"；判据单调会重试，降级为警告。
   LevelDB/RocksDB 的删除同样是尽力而为。
3. **快照编辑项必须与版本捕获在同一个 db.mu 临界区**，否则副本 Manifest 会引用
   没复制过去的文件。副本 Manifest 编号 = 源 nextFileNum 且 edit.NextFileNum 自增防自撞。
4. **gotest-race.sh 自己会写 _race.log**——外面再 `> _race.log` 重定向会双写互踩，
   日志被刷成千万行重复。重定向必须换个文件名。
5. **Bash 工具的 heredoc/`python -c` 会把 `\n` 转成真换行**（工具层转义干扰），
   复杂文本生成一律用 Write 写 .py 脚本再执行；另外 Edit 工具偶尔与磁盘状态不同步
   （改完 grep 不到），改完必须 grep 复核。
